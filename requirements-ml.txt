# Heavy ML dependencies - install separately for faster builds
# These are the slowest packages to install

# Core ML framework - CPU only to reduce memory usage during build
torch==2.0.1

# NLP and transformers
transformers>=4.30.0
sentence-transformers>=2.2.0
spacy>=3.0.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Visualization
pydeck>=0.8.0

